public class Point {
	public Double x, y;
	
	//constructor of Point class with X and Y coordinate declaration
	Point(Double x, Double y) {
		this.x = x; //changed here y to x
		this.y = y;
	}
}
